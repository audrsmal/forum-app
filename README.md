# forum-app (Next.js Pages Router + TypeScript)

## Setup
```bash
npm install
cp .env.local.example .env.local
npm run dev
```

Runs on http://localhost:3001

Backend base URL is set in NEXT_PUBLIC_API_URL (default: http://localhost:3000/api).
